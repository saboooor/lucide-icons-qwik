import { BaseIcon, IconProps } from '{{BASE_ICON_REL_PATH}}';
import { component$ } from '@qwik.dev/core';
{{DOCS}}
export default component$((props: IconProps) => {
  return <BaseIcon name="{{ICON_NAME}}" {...props}>
    {{CONTENT}}
  </BaseIcon>;
});
